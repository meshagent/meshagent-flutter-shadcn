import 'package:flutter/widgets.dart';

class LanceViewer extends StatelessWidget {
  const LanceViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("File preview not supported"));
  }
}
